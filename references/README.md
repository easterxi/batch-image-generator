# Reference image

Put your source image here as `reference.png`, or keep it private in Google Drive and pass its path with `--reference`.

The default pipeline resizes it to the target canvas and converts it to a Canny edge map before ControlNet conditioning.
